{
  "compilerOptions": {
    "module": "commonjs",
    "target": "es5", // 最终要转换成es5
    "allowJs": true // 允许使用js编码
  },
  "include": [
    "./src/*" // 要用ts-loader解析的文件路径
  ],
  "exclude": [
    "./node_modules" // 不用ts-loader解析的文件路径
  ]
}
